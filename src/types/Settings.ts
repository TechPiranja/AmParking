// this enables type safety in order to get all settings from the internal storage
export enum SETTINGS {
    SpeechVolume = "SpeechVolume",
    Radius = "Radius",
    PseudoNavigation = "PseudoNavigation"
}